version: "3.9"

services:

  frontend:
    build: ./frontend
    container_name: festival_frontend
    ports:
      - "8080:80"

  backend:
    build: ./backend
    container_name: festival_backend
    ports:
      - "5000:5000"